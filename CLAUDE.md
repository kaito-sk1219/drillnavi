# CLAUDE.md

DrillNavi のリポジトリで作業するときの指示。**作業開始前に必ず `docs/DESIGN.md` を読むこと。**

## プロジェクト概要

小学生〜中学生向けの学習支援 Web アプリ。CLI 版から Web アプリへの作り替えを進めている。

- **メインターゲット**: 勉強を後回しにする子と、それを見ている学童スタッフ・保護者
- **中核の価値**: 「今日やる分」を1画面に確定させ、始めるまでの心理的コストをゼロにすること
- **2つのモード**: grow（学力の伸長／目標正答率75%）と fun（着火と習慣化／目標正答率87.5%）

## 現在の状態

Phase 1（動くドリル）の途中。`src/drillnavi/core/elo.py` は実装・検証済み。
Web 層（FastAPI / Next.js）は未着手。旧 CLI 版のコードが一部残っている。

## 開発コマンド

```bash
uv sync                      # 依存関係の同期
uv run pytest                # テスト全実行
uv run pytest tests/test_elo.py -q   # 単一ファイル
uv run python experiments/sim_elo.py # Eloのシミュレーション検証
```

## アーキテクチャの不変条件

以下は**破ってはいけない**。破る必要が出たら、勝手に変えずに必ず相談すること。

1. **`src/drillnavi/core/` は純粋ドメイン層**
   I/O・DB・HTTP・グローバルな乱数状態・現在時刻の取得を一切含めない。
   乱数が必要なら `random.Random` を引数で受け取る。時刻が必要なら引数で受け取る。
   → アルゴリズムを差し替えながらシミュレーションで検証するために必須。

2. **問題の実体を DB に保存しない**
   問題は `(template_id, seed)` から決定論的に再生成する。同じ組から常に同じ問題が出ること。
   → 数百万行の問題テーブルを持たずに済み、リプレイと複数端末同期が成立する。

3. **`Attempt`（解答履歴）は追記のみ。更新・削除しない**
   theta も streak も、すべて Attempt から再計算できる状態を保つ。
   → アルゴリズムを変えたときに過去データで再評価できる。実用アプリでは死活的に重要。

4. **解答形式は `answer_type` で抽象化する**
   `integer` / `fraction` / `decimal` / `choice` / `text` / `kanji`。
   `int` 決め打ちのコードを書かない。旧 CLI 版はここで詰んだ。

5. **学習者の主キーは UUID。表示名を主キーにしない**
   旧 CLI 版は児童名をキーにしていて同名衝突と改名で履歴消失のバグがあった。

6. **Elo のパラメータを勘で変えない**
   `DEFAULT_K_INITIAL` / `DEFAULT_K_DECAY` は `experiments/sim_elo.py` のグリッド探索で
   決めた値（`docs/ELO_VERIFICATION.md` 参照）。変更する場合は必ずシミュレーションを回し、
   結果を同ドキュメントに追記してから変える。

## コーディング規約

- Python 3.12 / 型注釈は必須 / `from __future__ import annotations` を先頭に置く
- docstring は日本語。「何をするか」でなく「なぜそうするか」を書く
- ユーザー向け文字列は日本語。小学生が読む文字列は漢字にふりがなを想定した表現にする
- `core/` の関数には必ずユニットテストを書く。境界値・異常系・不変条件を含める
- コミットメッセージは日本語で可。1コミット1論点

## やってはいけないこと

- `core/` に `import requests` や `import sqlalchemy` を持ち込む
- テストを通すためにテストの側を緩める（実装のバグを疑うこと）
- 「とりあえず動く」ための握りつぶし（`except Exception: pass` の類）
- 個人情報を増やす方向の設計変更（氏名必須化、生年月日の日付まで取得など）を独断で入れる
- 依存パッケージの追加を相談なく行う

## 相互レビューの運用（重要）

このリポジトリでは **実装役とレビュー役の2エージェントで相互チェックしながら進める**。
`.claude/agents/` に `implementer` と `reviewer` を定義してある。

**標準の進め方:**

1. タスクを受けたら、まず `implementer` サブエージェントに実装させる
2. 実装が終わったら、**必ず** `reviewer` サブエージェントに差分をレビューさせる
   レビュー役には実装役の意図を伝えず、コードと `docs/DESIGN.md` だけを見せる
3. reviewer が指摘した項目を implementer に修正させる
4. 2〜3 を、reviewer が「重大な指摘なし」と返すまで繰り返す（最大3周）
5. 3周しても収束しない場合は、論点を整理してユーザーに判断を仰ぐ

**レビューを省略してよいのは、typo 修正・コメント修正・書式変更のみ。**
ロジック、データモデル、アルゴリズムに触れる変更は例外なくレビューを通すこと。

reviewer には「問題がなければ通す」ではなく **「この変更が本番で壊れるとしたらどこか」** を
探させる。承認を急がせない。
